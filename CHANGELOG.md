# Changelog

The changelog for the **Neolink Home Assistant add-on**
(`privatecoder/neolink-ha-addon`) lives in
[`neolink/CHANGELOG.md`](neolink/CHANGELOG.md) — the file Home Assistant reads and
displays inside the add-on. See it for the full version history and the
[`privatecoder/neolink`](https://github.com/privatecoder/neolink) image each
version wraps.
